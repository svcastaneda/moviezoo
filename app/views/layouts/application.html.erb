<!DOCTYPE html>
<html>
<head>
  <title>Movie Zoo</title>

	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <!-- Compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.5/css/materialize.min.css">

  <!-- Compiled and minified JavaScript -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.5/js/materialize.min.js"></script>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>


  <%= csrf_meta_tags %>
</head>
<body>
  <header>
	<nav class="top-nav teal">
    <div class="container">
      <div class="nav-wrapper teal">
  			<%= link_to 'Movie Zoo', root_path, class: 'page-title' %>
        <ul class="right">
            <li><%= link_to 'browse movies', new_movie_path %></li>
          <% if current_user -%>
  		      <li><%= link_to 'logout', logout_path, method: :delete %></li>
  		    <% else %>
  		      <li><%= link_to 'login', login_page_path %></li>
  		      <li><%= link_to 'create account', new_user_path %></li>
  		    <% end %>
        </ul>

      </div>
    </div>
  </nav>
  <div class="container"><a href="#" data-activates="nav-mobile" class="button-collapse top-nav full hide-on-large-only"><i class="mdi-navigation-menu"></i></a></div>
    <ul id="nav-mobile" class="side-nav fixed">
      <li><a href="#!">View All Movies</a></li>
      <li>Genres</li>
      <% get_genres.each do |genre| -%>
        <li class="genre">• <%= genre.title %></li>
      <% end %>
    </ul>
</header>



<main>
  <div class="container">
  <%= yield %>
  </div>
</main>
</body>
</html>
